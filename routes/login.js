/*const express = require ('express');
const router = express.Router()

router.get('/', (req,res) => {
    res.send('Welcome to this awesome API!')
})

router.get('/people', (req,res) => {
    res.status(200).json(data)  // endpoint: lets return the data
})
*/